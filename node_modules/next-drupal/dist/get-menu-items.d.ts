import { DrupalMenuLinkContent } from "./types";
export declare function getMenu(name: string, options?: {
    deserialize?: boolean;
}): Promise<{
    items: DrupalMenuLinkContent[];
    tree: DrupalMenuLinkContent[];
}>;
